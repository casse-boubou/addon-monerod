{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["github>casse-boubou/ha-addon-workflows//.github/renovate.json"],
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": ["/(^|/)Dockerfile$/"],
      "datasourceTemplate": "repology",
      "packageNameTemplate": "debian_13/{{package}}",
      "depNameTemplate": "{{package}}",
      "matchStrings": [
        "\\s(?<package>[a-z0-9][a-z0-9-_]+)=(?<currentValue>[a-z0-9-:_+~.]+)\\s"
      ],
      "versioningTemplate": "loose"
    },
    {
      "customType": "regex",
      "managerFilePatterns": ["/(^|/)Dockerfile$/"],
      "datasourceTemplate": "github-tags",
      "packageNameTemplate": "monero-project/monero",
      "depNameTemplate": "MoneroD",
      "matchStrings": [
        "(ARG)\\s(MONEROD.*_VERSION)=\"(?<currentValue>v?[0-9]+\\.[0-9]+(\\.[0-9]+){0,2})\"\\n"
      ],
      "versioningTemplate": "loose"
    }
  ],
  "packageRules": [
    {
      "matchDepTypes": ["action"],
      "pinDigests": false
    },
    {
      "matchDepTypes": ["workflow"],
      "pinDigests": false
    },
    {
      "matchManagers": ["custom.regex"],
      "matchDatasources": ["github-tags"],
      "matchDepNames": ["monero-project/monero"],
      "commitMessagePrefix": "✨",
      "commitMessageTopic": "{{depName}}"
    }
  ]
}
