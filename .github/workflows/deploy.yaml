---
name: Deploy

# yamllint disable-line rule:truthy
on:
  push:
    branches:
      - main
  release:
    types:
      - published
  workflow_dispatch:

permissions: {}

jobs:
  ci:
    permissions:
      actions: read
      contents: read
      packages: read
      security-events: write
    uses: casse-boubou/ha-addon-workflows/.github/workflows/app-ci.yaml@adfb21c360f53273c5ad8e389f7e976a819d7e42 # main

  deploy:
    needs: ci
    permissions:
      contents: read
      packages: write
    uses: casse-boubou/ha-addon-workflows/.github/workflows/app-deploy.yaml@adfb21c360f53273c5ad8e389f7e976a819d7e42 # main
    secrets:
      DISPATCH_TOKEN: ${{ secrets.DISPATCH_TOKEN }}
