---
name: Lock

# yamllint disable-line rule:truthy
on:
  schedule:
    - cron: "0 9 * * *"
  workflow_dispatch:

permissions:
  issues: write
  pull-requests: write

jobs:
  workflows:
    uses: casse-boubou/ha-addon-workflows/.github/workflows/lock.yaml@a282ba79666926dfbfba02cf92390ae73832f0fc  # main
